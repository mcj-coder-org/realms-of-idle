# Git configuration for feature branches
# This file is used by create-feature-branch.sh to set up branch-specific git config

# Use contributor account for feature branches
user.name = mcj-codificer
user.email = m.c.j@live.co.uk

# Enable GPG signing for feature branches
commit.gpgsign = true

# Configure commit template
commit.template = .github/commit_template.txt

# Set default push behavior to simple
push.default = simple

# Enable pull.rebase for cleaner history
pull.rebase = true