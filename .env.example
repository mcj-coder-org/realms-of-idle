# Authoritative Port Configuration
# This file is the single source of truth for all service ports.
# AppHost reads these to bind service ports.
# E2E tests read these for endpoint URLs.
# Copy to .env to use (or .env will be created from this template).

# === Service Ports (read by AppHost) ===
API_HTTP_PORT=5214
ORLEANS_HTTP_PORT=5001

# === E2E Test Endpoints (derived from service ports) ===
E2E_API_BASE_URL=http://localhost:5214
E2E_ORLEANS_BASE_URL=http://localhost:5001

# === Aspire Dashboard (from AppHost launchSettings.json) ===
E2E_ASPIRE_BASE_URL=https://localhost:17103
E2E_ASPIRE_RESOURCE_URL=https://localhost:22241

# === Timeouts ===
E2E_TIMEOUT_SECONDS=30
